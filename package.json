{
  "name": "demo-static-mirai-foundry",
  "version": "1.0.0",
  "description": "Mirai Foundry static website",
  "main": "index.html",
  "scripts": {
    "dev": "browser-sync start --server --files '**/*.html, **/*.css, **/*.js' --no-notify",
    "start": "browser-sync start --server --files '**/*.html, **/*.css, **/*.js' --no-notify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^3.0.0"
  }
}
